export interface LayerGeneratorItem {
    value: number;
    position: [number, number, number];
    positionRelative: [number, number, number];
    indices: [number, number, number];
    index: number;
}